module.exports = {
    port: process.env.PORT || 80 /* The PORT for the webserver. */,
    password: "root" /* The authorization key you will be using so that no random people can upload to your CDN. */
}